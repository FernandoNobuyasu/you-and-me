<audio class="bg-music" id="bg-music" controls loop>
    <source src="/you-and-me/music/sweather-weather.mp3" type="audio/mpeg">
    Seu navegador não suporta áudio HTML5.
</audio>

<style lang="scss">
    .bg-music{
        height: 25px;
        opacity: 0.5;
        margin-inline: auto;
        display: block;
        @media screen and (min-width: 600px) {
          position: fixed;
          bottom: 10px;
          right: 10px;
          z-index: 1000;
        }
    }
</style>

<script>
    const bgMusic = document.getElementById('bg-music');
  
    function playMusicOnce() {
      if (bgMusic) {
        (bgMusic as HTMLAudioElement).volume = 0.05;
        (bgMusic as HTMLAudioElement).play();
        document.removeEventListener('click', playMusicOnce);
        document.removeEventListener('scroll', playMusicOnce);
      }
    }
  
    document.addEventListener('click', playMusicOnce);
    document.addEventListener('scroll', playMusicOnce);
</script>